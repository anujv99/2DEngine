
global MakeTetrisLogic = function(tetrisGrid) {

	TetrisLogic = {
		TetrisGrid,
		PositionArray,
		ActiveShape,
	};

	TetrisLogic.Init = function(tetrisGrid) {
		.TetrisGrid = tetrisGrid;
	};

	TetrisLogic.Update = function() {
		
	};

	TetrisLogic.Init(tetrisGrid);

	return TetrisLogic;
};
